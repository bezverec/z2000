# ISO Coverage Scorecard

This document tracks how close z2000 is to ISO/IEC 15444-style JPEG2000
compatibility. The percentages are engineering estimates, not formal
conformance claims. A point is counted only when the feature is implemented,
fail-closed where unsupported, covered by local tests, and has at least one
interop or strict-reader check when the feature is externally visible.

## Current Snapshot

Last updated: 2026-07-12.

| Target | Score | Meaning |
| --- | ---: | --- |
| Narrow RGB lossless JP2 target | 100 / 100 | Single-tile RGB TIFF 6.0 to JP2, RCT, reversible 5/3, RPCL, BYPASS, one or more quality layers, PLT/TLM, strict z2000 decode, and OpenJPEG/Grok/Kakadu/jpylyzer smoke acceptance. |
| Full JPEG2000 Part 1 codec family | 95 / 100 | Broad Part 1 encode/decode coverage across tiles, progressions, quantization, irreversible profiles, code-block styles, rate allocation, and robust interop. |

The narrow target is intentionally much closer than the full-codec target. It
measures the practical archival path we are building first. The full-codec
target counts breadth across Part 1 features and should move slowly until
lossy profiles, tile-aware rate allocation, broader marker/component layouts,
and more decoder coverage exist.

## Narrow RGB Lossless Target

| Area | Weight | Current | Evidence | Next gate |
| --- | ---: | ---: | --- | --- |
| JP2 boxes and RGB metadata | 8 | 8 | Signature, `ftyp`, `jp2h`, `ihdr`, `colr`, contiguous `jp2c`, sRGB and restricted ICC preservation, plus `LBox == 0` and `XLBox` codestream box lengths. | Build a JP2/ICC interop fixture matrix and continue hardening malformed box diagnostics. |
| TIFF 6.0 RGB input/output | 7 | 7 | Uncompressed chunky RGB strips, 8/16-bit samples (16-bit now has a full-pipeline archival encode->strict-decode roundtrip plus confirmed OpenJPEG 2.5.4 / Grok 20.3.6 lossless interop and jpylyzer-valid output), ICC tag preservation, multi-strip metadata validation, and explicit fail-closed coverage for unsupported compressed, palette/unsupported photometric, planar RGB, extra-sample/alpha, mixed-bit-depth, signed-sample-format, and tile-only TIFF variants. | Add tiled TIFF support only as a later broadening step; the narrow path is now explicit about unsupported TIFF features. |
| Core main markers | 10 | 10 | `SIZ`, `COD` including layer-count and explicit precinct-byte policy, reversible `QCD` style/count/exponent validation, irreversible scalar-expounded `QCD` step-size validation for the public 9/7 path, single-tile profile validation, strict fail-closed coverage for unsupported CAP/PLM/RGN/POC/PPM/CRG main-header marker segments plus unsupported tile-part RGN/POC/PPT and override markers, duplicate `SIZ`/`COD`/`QCD`/`TLM` rejection, and TLM ordering checks that require COD+QCD context before TLM in both the raw strict reader and JP2 wrapper. | Keep marker validation synced with every newly accepted profile option. |
| Tile-part markers | 10 | 10 | `SOT`, `SOD`, `EOC`, `TLM`, `PLT`, optional `SOP`/`EPH`, resolution tile-parts, JP2-boundary sequential `SOT` audit through `EOC`, `TLM/Psot` length matching, `PLT` packet-span matching against `SOD` payload bytes, packet-marker policy checks from `COD/Scod`, deterministic rejection when a tile-part header marker is moved into packet payload, and independent no-sidecar smoke acceptance by OpenJPEG/Grok/Kakadu plus jpylyzer. | Keep a non-authoritative validator gate and investigate any future PLT/TLM warnings against independent decoders and the strict reader. |
| RCT and reversible 5/3 DWT | 10 | 10 | Lossless RCT and integer 5/3 encode/decode paths with strict roundtrip checks, including an odd/thin/minimal-dimension matrix (1x1, 5x5, 17x13, 3x100, 100x3, 255x1, 33x31, 63x65, 127x129, 2x2) that stresses the odd 5/3 DWT edge lifting, sub-band/precinct/code-block edge derivation, and the resolution clamp — each byte-exact through z2000 strict decode and confirmed lossless through OpenJPEG 2.5.4 and Grok 20.3.6. | Expand edge-tile coverage as multi-tile v2 lands. |
| T1/EBCOT/MQ for this profile | 20 | 20 | Continuous MQ-backed code-block payloads, ISO MQ default backend, direct MQ hot path, cleanup run mode, sign/refinement contexts, partial-prefix decode helpers, and BYPASS raw/MQ segments. Vertical-causal, segmentation-symbols, terminate-all, BYPASS+TERMALL, standalone and TERMALL-scoped reset-context, and TERMALL-scoped predictable termination are public opt-in profiles with focused local coverage; standalone RESET has bidirectional OpenJPEG/Grok `-M 2` lossless interop. The Kakadu style matrix now decodes z2000 RESET, TERMALL, RESET+TERMALL, ERTERM+TERMALL, BYPASS+TERMALL, and CAUSAL+SEGMARK files pixel-exactly, and z2000 decodes Kakadu RESTART, RESET+RESTART, ERTERM+RESTART, BYPASS+RESTART, CAUSAL+SEGMARK, and uniform QCD/COC override files pixel-exactly. Standalone ERTERM (COD `0x10`, ER-TERM final flush of the continuous MQ stream) is public opt-in with bidirectional Kakadu interop: kdu_expand decodes z2000 `--predictable-termination` output and z2000 decodes kdu `Cmodes=ERTERM`, `{ERTERM|RESET}`, and `{ERTERM|CAUSAL|SEGMARK}` files pixel-exactly. TERMALL/RESET+TERMALL/ERTERM/BYPASS+TERMALL also have a fail-closed corruption matrix (PLT segment-length flip, truncation, payload byte-flip walk, and an explicit over-capacity packet-header segment-count check). The aligned multi-tile path now covers CAUSAL+SEGMARK, RESET+TERMALL, ERTERM+TERMALL, and BYPASS+TERMALL with strict roundtrip, malformed-input/PLT corruption gates, and pixel-exact OpenJPEG/Grok/Kakadu decode, plus standalone RESET/ERTERM/ERTERM+RESET with strict multi-tile roundtrips and pixel-exact kdu_expand decode of genuine multi-tile output. The final narrow corpus gate encodes sparse, dense/sign-heavy, and refinement-heavy RGB inputs without BP8 sidecars, verifies strict block catalogs contain real multi-pass/multi-bitplane T1 payloads, and strict-decodes byte-exactly. | Keep unsupported style combinations fail-closed and reduce decode hot-path cost. |
| T2 RPCL packetization | 15 | 15 | Packet headers, tag-trees, `numlenbits`, layer deltas, RPCL indexing, strict SOD block catalog, packet rollback tests, subband-local precinct projection, no-sidecar rate-targeted layer-delta audit with deterministic first-packet and later-layer packet-header corruption rejection, and a multi-layer truncation regression where `PLT`/`SOT`/`TLM` accounting is shortened consistently but the strict packet catalog still rejects the incomplete packet as `TruncatedData`. | Keep packet truncation interop stable and extend the same discipline to future progression orders. |
| z2000 strict decode | 10 | 10 | No-sidecar strict RPCL/RCT/5-3 decode reconstructs z2000-produced ISO-MQ smoke files; ISO-MQ BP8 debug sidecar validation reuses the same strict SOD packet block catalog after byte-for-byte shadow-stream checks; BYPASS no-sidecar streams now prove that strict block catalogs carry style metadata, segment lengths, and payload views without the private BP8 oracle; consistent-truncation and marker-phase corruption cases fail deterministically. | Keep this gate green as future style combinations and packet orders are added. |
| Independent decoder interop | 10 | 10 | OpenJPEG, Grok, and Kakadu decode current no-sidecar output losslessly in local smoke tests; jpylyzer 2.2.1 reports the JP2 as valid with no warnings; pixels match the source TIFF. Output byte size is within about 0.06% of Grok/OpenJPEG/Kakadu on the local 3520x5115 smoke profile. | Keep commands/results reproducible and add a small fixture matrix for ICC-present and ICC-absent source TIFFs. |
| **Total** | **100** | **100** |  |  |

## Full Part 1 Codec Family

| Area | Weight | Current | Missing breadth |
| --- | ---: | ---: | --- |
| Containers and metadata | 10 | 8 | Basic JP2 boxes are accepted by jpylyzer for the current no-sidecar smoke file, restricted ICC preservation exists, standard `LBox == 0`/`XLBox` lengths parse for codestream boxes, and uniform unsigned RGB `BPCC` precision boxes parse when `ihdr` uses BPC=255. Top-level `xml `/`uuid`/`uinf` metadata boxes are accepted anywhere after `ftyp` (jpylyzer-valid fixture decodes through z2000), multiple `colr` boxes follow the I.5.3.3 first-supported-specification rule with PREC/APPROX treated as informative (APPROX 0..4), identity RGB `cdef` channel definitions are accepted (alpha/reassociation fail closed), and `res ` superboxes are structurally validated (resc/resd, nonzero ratios) instead of blindly skipped. Palette (`pclr`/`cmap`), alpha channels, mixed per-component precision, broader colourspace conversion, and JPX remain missing or unsupported by design. |
| Core codestream syntax | 15 | 14 | Multi-tile SIZ/SOT layouts, disabled MCT, all five Part 1 progression orders on single-tile and reference-grid-partitioned multi-tile paths, and main-header COC/QCC component markers are public in the bounded RGB policy: byte-redundant markers are accepted, and uniform COC/QCC overrides across all three RGB components are accepted when the signalled COD/QCD style is otherwise supported. Precincts, code-blocks, and tag-tree leaves retain their global partition origins; reversible 5/3 lifting tracks odd tile origins across decomposition levels. Partial/divergent per-component overrides plus tile-part COD/QCD and COC/QCC fail closed; malformed COC `Scoc`/SPcoc coding bytes and QCC qstyle fail as invalid in both paths, with shortened COC/QCC length bounded as truncation in the raw reader and invalid at the JP2 boundary. More marker variants (POC/RGN/CRG), arbitrary component layouts, and remaining style combinations remain. |
| Lossless encode profiles | 15 | 15 | Multi-tile emits all five Part 1 progression orders with quality layers in the one-part layout; OpenJPEG/Grok decode three-layer LRCP/RLCP/PCRL/CPRL plus the established RPCL profile losslessly. All direct tile-part divisions are now public on matching orders: `R`/RPCL, `L`/LRCP, `C`/CPRL, and `P`/PCRL. The 16-tile `C` gate emits three component parts per tile; the 16-tile `P` gate emits 256 position parts with per-tile reference-grid grouping. Both decode pixel-exactly through z2000, OpenJPEG, Grok, and Kakadu. The `R` 17x17 odd-origin gate remains pixel-exact through all four decoders. Reference-grid precinct/code-block anchoring and odd-origin 5/3 lifting are interop-green through all three references. Rate-targeted multi-tile encoding uses **global cross-tile PCRD**: one slope threshold over every code-block of every tile with layer byte targets referenced to the whole-image compressed payload (per-tile streams rebuilt from the global truncations; a single-tile grid reduces exactly to the former local allocation). The heterogeneous-grid regression pins the global budget landing at or under the target while individual tiles deviate from their proportional shares, and the 2048x2048 `--rates 20,10,1` LRCP smoke decodes pixel-exactly through z2000 strict decode, OpenJPEG, Grok, and Kakadu with a working `opj -l` layer prefix. The multi-tile resilience matrix includes CAUSAL, SEGMARK, RESET+TERMALL, ERTERM+TERMALL, BYPASS+TERMALL, and standalone RESET/ERTERM/ERTERM+RESET with strict deterministic roundtrips; representative profiles are pixel-exact through independent decoders. Remaining refinements (packet-header charging against multi-tile targets, rate-targeted style/progression matrix breadth) are quality polish, not missing capability. |
| Lossless decode profiles | 15 | 15 | Multi-tile z2000 streams decode per tile through the public strict path, including PLT-less packet spans derived from tile-local T2 headers and PLT-backed RPCL resolution parts joined into one tile catalog before T2/T1 reconstruction. Tile packet plans retain reference-grid resolution bounds and precinct origins instead of assuming every tile begins on a local partition boundary. Foreign OpenJPEG, Grok, and Kakadu lossless JP2s decode pixel-identically for PLT-backed streams and the single-tile PLT-less matrix, plus explicit-precinct, default-precinct, and 17x17 odd-origin multi-tile streams. The odd-origin matrix exercises clipped code-block/tag-tree partitions, origin-aware 5/3 lifting, and present geometry-empty packet headers; Kakadu's reordered one-part tile sequence is accepted when each tile appears exactly once, and foreign multi-part tile sequences now decode: per-part PLT packet accounting joins each tile's parts in TPsot order (grouped or interleaved across tiles), TNsot 0 "count not signalled" parts complete via packet accounting at EOC, empty SOT+SOD padding parts need no PLT, and joined non-RPCL tiles reorder once assembled — pinned by two embedded Kakadu fixtures (ORGtparts=L interleaved TNsot-0 parts; TNsot=8 empty padding plus 32-entry TLM) decoding the 64x64 gradient exactly, plus a live kdu matrix (16- and 32-part tiles, LRCP/RPCL/CPRL-3layer/ERTERM, 2048x2048 noise, all pixel-exact). Arbitrary component layouts, subsampling, and non-empty PLT-less multi-part tiles are tracked in the syntax/component rows. |
| Lossy encode/decode | 15 | 14 | ICT/9-7 with scalar-expounded and scalar-derived quantization exists for the narrow single-tile path with OpenJPEG-matching reconstruction; z2000 also decodes foreign OpenJPEG 2.5.4 9/7 lossy JP2s byte-identically to OpenJPEG across a moderate rate ladder (-r 1..8, pinned by an embedded-fixture regression test), covers a heavily truncated OpenJPEG `-r 10` ISO-MQ fixture with deterministic decode plus an explicit error bound, and decodes embedded Grok 20.3.6 `-I -r 8` and Kakadu 8.4.1 `Creversible=no -rate 3` JP2s whose scalar-expounded QCD mantissas differ from z2000's generated table — the Kakadu fixture also signals one guard bit and carries `res `/`resc` wrapper boxes. Strict irreversible QCD decode accepts signalled scalar-expounded/scalar-derived step sizes, guard bits 1..7, and uses their mantissas for dequantization. Rate-driven layers use global PCRD allocation (J.14), charge measured packet-header overhead against the targets, and now have a matched-byte PSNR ladder regression; current z2000 encoder quality trails OpenJPEG by 0.69-1.78 dB across the 256x256 mixed corpus layer prefixes. Truncated-plane midpoint reconstruction matches the references: foreign 9/7 agreement is ~50-55 dB / max byte diff 1-3 across the OpenJPEG and Grok -r 2..24 ladders and the Kakadu -rate 1..8 ladder (z2000 vs kdu_expand: max 2-3 / 51-55 dB). All three reference encoders have pinned decode fixtures, and a six-case CI reference-relative matrix (OpenJPEG -r 4/16, Grok -r 4/16, Kakadu -rate 4/1) embeds each reference decoder's own raster and recomputes z2000-vs-reference agreement on every run (max byte diff <= 3, PSNR >= 50 dB, cross-thread determinism; measured max 0-2 / 55-inf dB). **Multi-tile lossy is public now (2026-07-12):** the irreversible ICT+9/7+deadzone front end hooks into the transform-agnostic tile pipeline with per-band Mb from the signalled step exponents and origin-aware lifting across arbitrary viable tile origins. Scalar-expounded/derived roundtrips, a 2048x2048 512-tile smoke, and a 17x17 odd-origin gate are reference-relative through kdu_expand/opj_decompress/grk_decompress; both encode and foreign-decode odd-origin legs stay within max byte diff 1. Rate-targeted 9/7 multi-tile encoding is public too: the global cross-tile PCRD allocation weights each band by (reconstruction step delta x 9/7 synthesis norm)^2 (identical per tile), verified by a rate-targeted multi-tile 9/7 roundtrip (lossless final layer, cross-thread determinism, monotone per-layer PLT byte growth) and a 2048x2048 512-tile --rates 20,10,1 smoke whose layer prefixes progress 11.6->12.3->53 dB (opj -l) and whose full decode kdu_expand/opj_decompress/grk_decompress reconstruct within max byte diff 1 of z2000. Remaining encoder-side lossy work is to close the PCRD PSNR gap. |
| T1 completeness | 15 | 15 | All six Part 1 code-block style bits — BYPASS (0x01), RESET (0x02), TERMALL (0x04), vertical-causal (0x08), predictable termination (0x10), and segmentation symbols (0x20) — are public opt-in profiles in every combination (0x00..0x3f): RESET restarts the MQ contexts at coding-pass boundaries in the continuous, BYPASS, TERMALL, and BYPASS+TERMALL segment models, and ERTERM terminates every raw/MQ segment predictably (ER-TERM MQ flush; alternating-bit raw padding per opj_mqc_bypass_flush_enc, kept after a stuffed 0xff). Interop: the BYPASS+RESET/BYPASS+ERTERM/BYPASS+RESET+ERTERM and BYPASS+TERMALL+RESET/ERTERM combinations plus the full 0x3f style decode pixel-exactly through kdu_expand, opj_decompress, AND grk_decompress on the 2048x2048 encode leg, and z2000 strict decode reconstructs kdu Cmodes={BYPASS|RESET}, {BYPASS|ERTERM}, {BYPASS|RESET|ERTERM}, {BYPASS|RESET|CAUSAL}, {BYPASS|RESET|RESTART}, {BYPASS|ERTERM|RESTART}, and the all-six-bit file pixel-exactly. Earlier landed interop (TERMALL/RESET+TERMALL/ERTERM+TERMALL/BYPASS+TERMALL smokes, standalone RESET bidirectional opj/grok -M 2, standalone ERTERM bidirectional Kakadu, the fail-closed corruption matrix, and the multi-tile style coverage) is unchanged. The legacy MQ backend keeps its own fail-closed gates. |
| T2 completeness | 10 | 9 | All five Part 1 progression orders are public with OpenJPEG/Grok lossless interop (single- and multi-layer). Tile-part divisions cover none plus every direct Part 1 mode on a matching contiguous order: per-resolution `R` (RPCL), per-layer `L` (LRCP), per-component `C` (CPRL), and per-position `P` (PCRL), each with per-part PLT/TLM accounting and persistent SOP/T2 state. The `L`, `C`, and 256-part `P` smokes decode pixel-exactly through z2000 strict decode, kdu_expand, opj_decompress, and grk_decompress; the general multi-part walk also consumes foreign grouped/interleaved/TNsot-0 layouts. Remaining: POC progression-order changes and PPM/PPT packed packet headers. |
| Interop and conformance gates | 5 | 5 | Reproducible OpenJPEG/Grok/Kakadu/jpylyzer matrix exists locally for the narrow smoke file, and a CI-enforced corruption-sweep gate fuzzes every parse surface (raw codestream and JP2-wrapped) across seven profiles — single-tile archival RCT/5-3, multi-tile RCT/5-3, irreversible ICT/9-7, terminate-all, BYPASS+TERMALL, multi-tile TERMALL, and multi-tile BYPASS+TERMALL: truncation at every length plus single-byte corruption across SIZ/COD/QCD/TLM/SOT/SOD/PLT/SOP/EPH/packet-header/tag-tree/T1 regions, asserting bounded handling with no panic or out-of-bounds read under Debug, ReleaseSafe, and ReleaseFast. Broader corpus fuzzing can still expand. |
| **Total** | **100** | **95** |  |

This full-codec score is intentionally strict. z2000 has useful pieces of a
Part 1 encoder already, but a general-purpose codec must handle many more
profiles and arbitrary external inputs before the percentage should climb fast.

## Counting Rules

- `0%`: no implementation, or only a parser flag that is not connected to
  payload behavior.
- `25%`: implemented internally with focused tests, but not exposed in the
  public codestream profile.
- `50%`: exposed fail-closed or supported for one narrow path with local tests.
- `75%`: supported in the narrow path and accepted by at least one independent
  implementation or strict z2000 decode.
- `100%`: covered by local tests, strict validation, malformed-input tests, and
  OpenJPEG/Grok/Kakadu-style interop for the supported profile.

When a feature becomes broader but less stable, prefer lowering the score until
the new surface is tested. The score should reward boring reliability, not just
more accepted command-line options.

## Update Checklist

Update this file whenever a PR changes one of these gates:

- A new JPEG2000 marker, box, progression order, transform, quantization style,
  code-block style, or tile-part mode becomes supported instead of fail-closed.
- OpenJPEG, Grok, Kakadu, or an external validator accepts or rejects the
  current output in a new way.
- z2000 can decode a broader class of external codestreams.
- Benchmarks become fairer because the output is accepted by independent
  decoders without warnings.
- A debug-only oracle path becomes unnecessary for normal encode/decode.

Keep a one-line note in `docs/changelog.md` for score changes that move either
top-level number by at least two points.

Validator notes:

- External validators are diagnostic gates, not absolute sources of truth.
  Treat any warning as a hypothesis to check against the strict reader, the
  Part 1 text, and independent decoders.
- ICC metadata is required only when the source TIFF contains an ICC profile.
  ICC-absent TIFF input should produce ICC-absent JP2 output without counting
  that absence as a failure.
